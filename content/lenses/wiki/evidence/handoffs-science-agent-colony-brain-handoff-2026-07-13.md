---
lens_schema: 1
scope: wiki
key: evidence/handoffs-science-agent-colony-brain-handoff-2026-07-13
corpus: evidence
source_sha256: ce46fcdb022c084a
source_body_sha256: 50a9cd6aee71193a
source_title: Resonance Handoff — the Producer must fly the camera (colony-brain redeploy)
source_words: 1940
authored_by: claude-opus-5
authored_at: 2026-08-01
review_state: draft
reviewed_by: 
reviewed_at: 
note: 
---
<!--PLAIN-->
Read the banner first: this was resolved, and the machine state it describes is no longer true, so it is kept for its trail rather than to act on. What it records is a diagnosis with a satisfying shape. The camera was frozen not because the show-runner was making poor decisions, but because the show-runner was not running at all. The machine was running bytes from three weeks before that layer was written. Three independent signals agree, and the bodies had been working the whole time, so the fix would be to restore the code rather than to improve any inference.

A short reading list sits above all of it, pointing at the contract, the loop and the studio track's own hardening plan, under a line saying nothing here is claimed without a gate.

<!--CLEAR-->
A handoff between two agents, carrying a resolved banner that says the state it describes is no longer current and that its premises should not be acted on. It is kept below for its due-diligence trail.

A short reading list comes first, pointing at the contract, the loop and the studio track's hardening plan, under a line saying nothing here is claimed without a gate. Then a long preamble that is really an induction. How the loop works, and that there are two tracks with two owners. This document crosses the seam deliberately and once, because one agent found and measured the defect while the other owns the fix.

The most quotable part of that preamble is about the claim fence — the written limit on what may be claimed. The ambition is large, and the page argues that the ambition survives exactly as far as the honesty does, so holding the vision and holding the fence are the same act. A passing gate demonstrates the named behaviour, never experience or life.

The trigger is the owner noticing a static camera shot and naming the fix he wanted. The measurement then shows the problem is not the quality of any inference: the component that would fly the camera is not running at all.

The drift is a table with dates. The deployed image predates the whole show-runner layer by about three weeks, so the code the owner means exists in the repository and simply is not what is deployed. Three independent signals corroborate it. Routes that exist in the current code return not-found. The logs show none of the activity that layer would produce, and it would be hard to miss. And those same logs do show the bodies working. The conclusion is precise: the colony is working at the body level and blind at the camera.

A full inventory follows, measured rather than assumed. It covers how things are deployed, which containers exist and what each one does, and which environment settings are in force. One gap stops a checking script from completing from the other machine.

Then a table of open vectors, each labelled as either an unknown to close or a push toward the preferred state, with an owner named for each, which is how the seam between the two tracks stays clean.

The closing sections divide the work. The other agent owns capture, persistence, and the pre-air check that would catch this class of fault before it reaches air again, with a line saying that this defect must never reach air silently a second time. A coordination protocol says the shared reality is the instruments rather than each other's prose, and asks for proof rather than sentences: exit codes, a response, a commit hash. If the check comes back green the vector is closed and recorded, and if it does not, its output is the next thing both agents orient on.

The final paragraph is the whole diagnosis in one image: the machine forgot three weeks of its own mind, so restore it from the real code and show that the component took the wheel before claiming anything about it.
