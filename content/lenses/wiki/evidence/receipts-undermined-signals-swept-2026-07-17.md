---
lens_schema: 1
scope: wiki
key: evidence/receipts-undermined-signals-swept-2026-07-17
corpus: evidence
source_sha256: b9bcb5eaa00fd681
source_body_sha256: 9c8d4a051b4ee3c4
source_title: Receipt — the undermined signal sweep (WS-B): every always-on panel made honest
source_words: 1670
authored_by: claude-opus-5
authored_at: 2026-08-01
review_state: draft
reviewed_by: 
reviewed_at: 
note: 
---
<!--PLAIN-->
A sweep over the panels an operator watches between tests, which had never received the fixes the tests themselves got. Nine signals were being displayed while lying or being fragile, and each fix was registered as pending, with its pass condition, before the change. The theme is that a hardened test does not help if the always-on surfaces still count bytes instead of pixels, report a hardcoded healthy, or grow greener as their sensor dies. One item is held at partial rather than claimed, because the rehearsal that would justify it does not exist yet.

<!--CLEAR-->
A sweep covering the surfaces an operator looks at between tests. Its opening observation is the reason it exists: the periodic test had been hardened, and the always-on panels had not, so the same defect classes were still live exactly where somebody would be looking.

Each item is written the same way. What it was, what the fix is, a pass condition and a falsifier, with the gate registered before the change.

The first is the discredited byte-count still live on the go or no-go route, hundreds of lines away from where the same thing had already been fixed. A picture of a black frame clears the threshold easily. The fix reuses the pixel classifier that already existed, along with the logic distinguishing an absent input from a black one, and a pass now also requires that at least one scene genuinely rendered, because everything being skipped proves nothing.

The second is a missing floor. With several destinations armed and one dark, the healthy one holds the measured count above the bar, so a dark platform reads green. The bar the periodic test already used is applied to the always-on panels too.

The third is a health endpoint that returned healthy as a literal, which something upstream trusted, so a wedged loop still reported up. It is now derived from how recently the loop actually ran.

Then a cluster about staleness. A frozen snapshot read as current for three independent reasons, including a stall detector that could not fire because it refreshed the value it then checked. A helper scanned minutes into the past for any value and returned it with no age, so an old number rendered in a confident colour. Freshness is now computed when the request is served, ages travel with values, stale values are greyed and dated, and one reading is forced to unknown past the threshold.

One item is especially memorable. A sensor had been dead for days and was launched by nothing, so its findings dropped out and the panel grew greener as it stayed dead. The fix renders the freshness flag and says the detectors are not reporting, and deliberately does not synthesise a bad finding for something that never reported at all.

Two smaller ones follow: a panel that pinned itself on a byte-identical recovery, and a count that counted records, including dead ones, rather than live processes. And a tile that claimed a public path was fine from a bare port check is reworded to say that a reachable port is not evidence of forwarding.

The proofs are decision tables comparing old and new behaviour case by case, alongside live readings. One is held at partial rather than passed, because the render-failure rehearsal that would justify it does not exist yet, and the receipt cites its own standing rule that no green claim about that part is permitted until it does. A closing section covers persistence across a restart, a step that had been manual becoming a measured one, a retired ceremony removed from a diagram, and a hard-coded address replaced by a name.
