---
lens_schema: 1
scope: wiki
key: control-plane/phases-phase-9-remediation
corpus: control-plane
source_sha256: 022aff08f0e858bd
source_body_sha256: 022aff08f0e858bd
source_title: Phase 9 — Return to resonance
source_words: 2036
authored_by: claude-opus-5
authored_at: 2026-08-31
review_state: draft
reviewed_by: 
reviewed_at: 
note: Re-read and re-bound 2026-08-31. The document's generated state blocks changed twice under this lens — the next act was repointed after an operator ruling of 2026-08-10 was finally transcribed into the plan, and a renderer defect that printed 'undefined' into the retired-token line was repaired. The opening paragraphs of both lanes were re-authored against the new text; everything describing the unchanged sections stands. review_state stays draft — the operator has never reviewed this lens and a re-bind cannot claim he has.
---
<!--PLAIN-->

This is the register for a phase still in execution: most steps done, one blocked, several planned, and three on the operator's desk — one of which was a checkpoint his own ruling later withdrew, and the register now says so. Its next act names two live working fronts, both open to agents: repairing the whiteboard's recorded defects, and assembling the public University. Several of its numbers are generated from the plan rather than typed, and the page explains why. An earlier version stated a status the other surfaces did not, which broke the plan's own law — and then did it again within a day.

The most distinctive thing here is the four-proof cycle, binding on every step. Prove it by making a test fail first, for the reason written down in advance. Re-measure with the instrument rather than the same test again. Then check it by a different method, named before execution, which must not import the code under test — and if that disagrees with the first proof, the different method wins and the disagreement is spoken first. Finally the operator proves it himself, with one command or one look, never a procedure.

Steps carried out without a row written down in advance are listed rather than left looking like inventions.

<!--CLEAR-->

A remediation phase still in execution keeps its register here. Its status line and its counts are generated from the plan file rather than written by hand, and the reason is recorded below them. An earlier version of this line stated a status the other surfaces did not, breaching the plan's own law that none of the three may state a status the others do not. And it breached the same law again within a day. Generating the counts is described as what that law was always asking for and never had a mechanism to enforce.

The generated header now records a withdrawal as well as completions: the checkpoint the phase once waited on was ruled out by the operator himself, and the retired name stays visible with how it ended. The declared next act is two agent fronts: the whiteboard defect programme and the public University.

A short paragraph then lists steps executed without ever gaining a row in the table written down in advance, recorded here rather than left looking like inventions. One was a necessary repair ruled on by the operator; the rest ran under the same standing authorisation. The page notes that one step's own failure sign is a step invented during execution, so that sign had read as fired and unanswered until this paragraph answered it. Recording them late is better than not recording them at all.

The section on why the phase exists is unusually direct. The author had recommended leaving the governing body half-built in order to go and do the science; the operator rejected that as a fatal error, and the page says he was right: it would leave the science ungoverned. Measurement then dissolved the choice, because the path to broadcast turned out to be blocked on a science gate. The road runs through the science.

The four-proof cycle binds every step. First, a test that fails first for the reason written down in advance, then the smallest fix; vacuous tests are marked as not counted. Second, re-measure — the instrument re-run from a clean copy against the baseline, where an unexpected second improvement is as suspicious as a failure. Third, a different method, named before execution, which must not import the code under test; if it disagrees, it wins, and the disagreement is spoken first. Fourth, the operator proves it himself with one command or one look, never a procedure.

A table then names, for every step, which independent method and which operator-facing form will be used, together with what would show it wrong. The methods range from mutation and independent reimplementation to a live probe, an adversarial agent, a historical replay, a negative control, and the operator's own eye.

The results of the first stage are then given, and the main finding is worth reading. A single response carried two mutually exclusive statements: a field claimed to report the commit the running code sits on, while it actually read the repository's head at request time. So the field that should have caught a long blind spot is the field that hid it. A table shows several long-lived processes serving code well behind the current head, including the supervisors themselves.

A retraction follows, settled by measurement: the author had reported that a check reported failure but exited successfully, and that was false — he had read the exit status of a command it was piped into. The real defect is different and stands: nothing invokes that check.

The page closes with numbered stop conditions and an exit condition requiring the next plan to exist.
